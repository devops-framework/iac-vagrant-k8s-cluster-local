name: 'Build and Push Docker Image'
description: 'Builds a docker image and pushes it to Docker Hub'
inputs:
  docker_username:
    description: 'Docker Hub Username'
    required: true
  docker_password:
    description: 'Docker Hub Password'
    required: true
  image_name:
    description: 'Name of the image'
    required: true
  image_tag:
    description: 'Tag of the image'
    required: false
    default: 'latest'
  dockerfile_path:
    description: 'Path to Dockerfile'
    required: false
    default: './Dockerfile'
  context:
    description: 'Build context'
    required: false
    default: '.'
outputs:
  dockerhub_url:
    description: 'Direct URL to the image tag on Docker Hub'
    value: ${{ steps.generate-url.outputs.url }}

runs:
  using: "composite"
  steps:
    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ inputs.docker_username }}
        password: ${{ inputs.docker_password }}

    - name: Build and push
      uses: docker/build-push-action@v5
      with:
        context: ${{ inputs.context }}
        file: ${{ inputs.dockerfile_path }}
        push: true
        tags: ${{ inputs.docker_username }}/${{ inputs.image_name }}:${{ inputs.image_tag }}

    - name: Generate Docker Hub URL
      id: generate-url
      shell: bash
      run: echo "url=https://hub.docker.com/repository/docker/${{ inputs.docker_username }}/${{ inputs.image_name }}/tags?name=${{ inputs.image_tag }}" >> $GITHUB_OUTPUT